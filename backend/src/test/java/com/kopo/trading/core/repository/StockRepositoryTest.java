package com.kopo.trading.core.repository;

import com.kopo.trading.core.entity.Stock;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.Date;
import java.util.List;

class StockRepositoryTest {
    StockRepository stockRepository;

//    @Test
//    void paramCheck() {
//        List<Stock> stocks = new ArrayList<>();
//        for (Long i = 0L; i < 3; i++) {
//            Stock stock = new Stock(new Date(), "1", i, i, i, i, i);
//            stocks.add(stock);
//        }
////        Assertions.assertThat(stocks.get(0)).isEqualTo(stockRepository.findByIdOrderByTradingDayAsc("1"));
//    }
}